import React from 'react';
import { Segment } from 'semantic-ui-react';

const Hero = () => (
  <Segment
    style={{
      height: '300px',
      backgroundColor: '#ddd',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      fontSize: '2rem',
    }}
  >
    Image
  </Segment>
);

export default Hero;
